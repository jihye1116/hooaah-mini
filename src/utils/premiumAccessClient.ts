'use client';

import {
  saveAccessPermission,
  hasAccessPermission,
  validateAccessCode,
} from '@/utils/premiumAccess';

export { saveAccessPermission, hasAccessPermission, validateAccessCode };
